# This is not the specification

The current specification for Make The Team lives at:

**`docs/superpowers/specs/2026-08-10-make-the-team-design.md`**

That document is the single source of truth. Build from it.

The original v1 draft (working name "kickabout") that used to occupy this path is
preserved for history at `docs/history/2026-08-10-original-draft-spec.md`. It is
superseded: it disagrees with the current specification in several places, and
those disagreements were resolved in favour of v2.

This stub exists so that nobody — person or agent — opens the obvious filename at
the repository root and builds from the superseded draft.
